# Drill fleet — the fake app (pilot.drill) on :8765, health toggled by hand.
#   task drill                              # start it
#   task pilot -- watch --targets drill.targets.yaml --fix --every 5
#   task drill -- wedged | down | heal      # toggle
targets:
  - name: drill
    url: http://127.0.0.1:8765
    manifest: drill.sconix.yaml
    root: .
